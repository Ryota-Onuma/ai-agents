{
  "mcpServers": {
    "postgresql": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "/Users/ryota/Desktop/programming/ai-agents/mcp/postgresql-mcp/main.py"],
      "env": {
        "POSTGRESQL_DEFAULT_URL": "${POSTGRESQL_DEFAULT_URL:-postgresql://postgres:password123@localhost:5432/primary_db}",
        "POSTGRESQL_PRIMARY_URL": "${POSTGRESQL_PRIMARY_URL:-postgresql://postgres:password123@localhost:5432/primary_db}",
        "POSTGRESQL_SECONDARY_URL": "${POSTGRESQL_SECONDARY_URL:-postgresql://postgres:password123@localhost:5433/secondary_db}",
        "POSTGRESQL_ANALYTICS_URL": "${POSTGRESQL_ANALYTICS_URL:-postgresql://postgres:password123@localhost:5434/analytics_db}"
      }
    }
  }
}